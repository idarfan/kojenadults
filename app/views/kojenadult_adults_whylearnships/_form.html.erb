<%= form_for(@kojenadult_adults_whylearnship) do |f| %>
  <% if @kojenadult_adults_whylearnship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kojenadult_adults_whylearnship.errors.count, "error") %> prohibited this kojenadult_adults_whylearnship from being saved:</h2>

      <ul>
      <% @kojenadult_adults_whylearnship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
